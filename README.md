# 🏥 Patient Pathway Analysis

End-to-end analysis of synthetic patient healthcare data — from relational database design to statistical inference and interactive dashboard.

Built as a portfolio project to demonstrate data management, SQL, and biostatistics skills applied to a healthcare context.

---

## Project Overview

This project explores patient care pathways using a synthetic clinical dataset (Synthea). Starting from raw CSV files, I built a normalised SQLite relational database, conducted descriptive and inferential analyses, and developed an interactive dashboard to visualise the results.

The central question: **what patient characteristics are associated with higher healthcare utilisation?**

---

## Repository Structure

```
patient-pathway-analysis/
│
├── data/
│   └── raw/                        # Raw Synthea CSV files (not tracked by Git)
│
│
├── notebooks/
│   ├── 01_exploration.ipynb        # Data ingestion, schema design, database initialisation
│   ├── 02_sql_analysis.ipynb       # SQL queries, KPIs, window functions, cohort description
│   └── 03_statistical_analysis.ipynb  # Inferential statistics and linear regression
│
├── plots/                          # All exported figures (generated by notebooks)
│
├── streamlit_app/
│   ├── app.py                      # Main entry point
│   ├── 1_Overview.py               # Cohort KPIs
│   ├── 2_Pathways.py               # Healthcare utilisation analysis
│   ├── 3_Patient_Explorer.py       # Individual patient journey
│   └── 4_Statistics.py            # Interactive statistical tests
│
├── requirements.txt
└── README.md
```

---

## Dataset

**Source:** [Synthea — Synthetic Patient Population Simulator](https://synthea.mitre.org/)  
**Type:** Synthetic (no real patient data)  
**Tables built:**

| Table | Description | Key |
|-------|-------------|-----|
| `patients` | Demographics (gender, age, city) | `patient_id` |
| `encounters` | Healthcare visits | `encounter_id` → `patient_id` |
| `conditions` | Diagnoses over time | `condition_id` → `patient_id` |
| `procedures` | Medical acts performed | `procedure_id` → `patient_id` |

---

## Notebooks

### `01_exploration.ipynb` — Database Initialisation
- Loading raw CSV files into pandas DataFrames
- Schema design with primary keys and foreign key constraints (`PRAGMA foreign_keys`)
- Data cleaning and column normalisation
- Referential integrity verification

### `02_sql_analysis.ipynb` — SQL Analysis & Healthcare KPIs
- Cohort description: gender, age distribution, top pathologies
- Healthcare utilisation: visits per patient, top consumers
- Multimorbidity analysis: patients with 5+ distinct conditions (`COUNT(DISTINCT)`, `HAVING`)
- Temporal reconstruction of care pathways using SQL window functions:
  - `ROW_NUMBER()` — visit chronology per patient
  - `LAG()` — time elapsed between consecutive visits
  - `RANK()` — patient ranking by healthcare intensity

### `03_statistical_analysis.ipynb` — Statistical Analysis
- Descriptive statistics with normality assessment (mean vs median, outlier detection)
- **Chi-square test** — gender vs complex chronic status
- **Mann-Whitney U test** — healthcare utilisation in obese vs non-obese patients (non-parametric, justified by skewed distribution)
- **Welch t-test** — younger vs older patients (CLT justification for skewed data)
- **OLS linear regression** — age as continuous predictor of total visits, with residual diagnostic plot (heteroscedasticity assessment)

---

## Key Findings

- The visit distribution is heavily right-skewed (mean: 58.6 visits, median: 36.0) — a small subset of patients drives a disproportionate share of healthcare consumption
- Patients with obesity show significantly higher healthcare utilisation than non-obese patients (Mann-Whitney, p < 0.05)
- Older patients accumulate significantly more visits than younger patients (Welch t-test, p < 0.05)
- Age is a statistically significant predictor of visit count, but explains only ~12% of variance (R² = 0.122) — other factors such as clinical phenotype and social determinants play a major role
- Gender is not significantly associated with complex chronic status in this synthetic cohort

---

## Streamlit Dashboard

The interactive dashboard provides four modules:

| Page | Content |
|------|---------|
| Overview | Cohort KPIs (patients, age, gender, visits) |
| Care Pathways | Visit distribution, top users, chronic patients |
| Patient Explorer | Individual clinical timeline by patient ID |
| Statistics | Chi-square, Mann-Whitney, regression results |

**To run locally:**

```bash
pip install -r requirements.txt
streamlit run streamlit_app/app.py
```

---

## Stack

- **Python** — pandas, NumPy, matplotlib, scipy, statsmodels
- **SQL** — SQLite (window functions, CTEs, subqueries)
- **Streamlit** — interactive dashboard
- **Jupyter Notebook** — analysis and documentation

---

## Methodological Notes

**Why 5+ conditions as the multimorbidity threshold?**  
Synthea tracks the full longitudinal history of each patient, including minor acute episodes. Using the standard 2+ threshold would classify nearly the entire cohort as multimorbid. The 5+ threshold isolates a genuinely complex subpopulation for meaningful analysis.

**Why Mann-Whitney instead of a t-test for visit comparison?**  
The distribution of total visits is heavily right-skewed with extreme outliers. The normality assumption required by Student's t-test is violated, making Mann-Whitney the appropriate non-parametric alternative.

**Why Welch instead of Student's t-test for age groups?**  
Welch's formulation does not assume equal variances between groups, which protects against type I error inflation when group variances differ — as is the case here.

---

## Author

**Juliette Bouli-Mengue**  
M1 Biologie-Santé — Université Paris-Saclay  
Attachée de Recherche Clinique — Unicancer  
[GitHub](https://github.com/juliettebm) · [LinkedIn](#)

---

*Educational project — synthetic data only, no real patient information.*
